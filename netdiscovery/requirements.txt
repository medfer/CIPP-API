networkx>=3.2
pyvis>=0.3.2
pytest>=7.0

# Real SNMP transport (LiveSnmpTransport). Optional for the simulated demo --
# only import netdiscovery.transport.LiveSnmpTransport requires it installed.
# Pinned below 7: pysnmp 7 removed the pysnmp.hlapi.asyncio module this
# transport is built on (folded into v1arch/v3arch, which also pulls in the
# full USM/crypto stack even for plain v2c use).
pysnmp>=6.1,<7; python_version >= "3.11"
